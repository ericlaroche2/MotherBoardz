package com.android.ericlaroche.products.Activity;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import com.android.ericlaroche.products.R;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);


        //TODO: make two buttons that allow user to goto list for products/tags
        //TODO: pass to two different separate activities
    }
}
